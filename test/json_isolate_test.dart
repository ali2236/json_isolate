import 'package:json_isolate/json_isolate.dart';
import 'package:test/test.dart';

void main() {
  group('A group of tests', () {
    JsonIsolate awesome;

    setUp(() {
      awesome = JsonIsolate();
    });

    test('First Test', () {});
  });
}
